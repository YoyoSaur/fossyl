# Server
PORT=3000

# Database — set DATABASE_URL for libsql server (Docker/production)
# or leave unset for local file-based SQLite (pnpm dev)
DATABASE_URL=http://sqld:8080
# DATABASE_PATH=./data/app.db  # uncomment for local dev override
